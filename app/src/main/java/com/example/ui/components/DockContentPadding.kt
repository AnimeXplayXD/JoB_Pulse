package com.example.ui.components

import androidx.compose.runtime.compositionLocalOf
import androidx.compose.ui.unit.dp

/** Clearance for the measured floating dock, including large-font layouts. */
val LocalDockContentPadding = compositionLocalOf { 120.dp }
